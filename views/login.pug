extends layout

block content
    form(action="/login", method="post")
        p
            input(name="userName", placeholder="请输入用户名", require="true")
        p
            input(name="password", placeholder="请输入密码", require="true", type="password")
        button 登录
        